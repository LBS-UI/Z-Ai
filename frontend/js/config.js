// ═══════════════════════════════════════════════════════════════════
// config.js — the ONE place to point the frontend at a different
// backend (e.g. when you deploy). Local dev needs no changes.
// ═══════════════════════════════════════════════════════════════════

window.ZIMELIA_API_BASE = 'http://localhost:3001/api';
